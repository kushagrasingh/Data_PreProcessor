Data PreProcessor :

This tool is used for pre-processing the training data. 
Its functions include detecting and discretization of data fields that contain continuous data and dividing the training data into internal training set and internal test set,
outlier detections, missing values in data sets, sampling and data reduction.

This project was built as a Data Mining course assignment.
